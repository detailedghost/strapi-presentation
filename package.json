{
  "name": "strapi-presentation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prod": "vite build --base '/strapi-presentation/' && rm -rf docs && mv dist docs"
  },
  "devDependencies": {
    "reveal.js": "^4.3.0",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "md5": "^2.3.0"
  }
}
